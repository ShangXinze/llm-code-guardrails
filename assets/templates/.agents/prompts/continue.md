继续当前任务。规则以 `AGENTS.md` + `CONSTITUTION.md` 为准，本文件只给动作顺序。

1. 热续读取：`TASK.md` + 上一步变更的文件 + 当前模块 `README.md`（不必全量重读）。
2. 先输出，再动手：

```
任务 ID：
当前进度（计划里第几步）：
已完成：
下一步：
风险：
是否 blocked：
距完成还差：
```

3. 只修改 `SCOPE:MODIFY` / `SCOPE:CREATE` 白名单内的文件。
4. 每完成一步就追加 `TASK.md`「进度日志」，跑相关验证。
5. 发现自己正在做计划外的事情（重构、修别的 bug、优化）→ **停**，记入「未决问题」，回到计划。
6. 计划已经不成立（任务拆分错了）→ 改 `TASK.md` 的计划并写明原因，同步 `ROADMAP.md`，再继续。
7. 需要扩权写「范围变更请求」后停止。
