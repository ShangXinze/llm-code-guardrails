# TASK.md — <任务 ID> <一句话标题>

> 当前唯一活跃的任务卡。任务开始时从 `ROADMAP.md` 队列取，完成后归档到 `{{TASKS_REL}}/`，本文件回到空白模板。
> 范围白名单**只在这里定义一次**；其他文档不得再声明一份。

## 元信息

- 任务 ID：M0-1（对应 `ROADMAP.md` 任务队列的一行）
- 所属模块：M0
- 状态：todo | in_progress | blocked | done
- 模式：execute | recon
- approval：human | auto（human 时复述后必须等人类确认才动代码）
- 负责人：
- 分支：
- 开始时间：{{DATE}}
- 最后更新：{{DATE}}

## 目标

一句话说明本任务要完成什么。不要写"优化一下""完善功能"。

## 非目标

- 不做什么。（写清楚，这是防偏移的第一道闸）

## 验收标准（每条必须可判定）

> 格式：`ACn：运行 <命令> → 期望 <输出>` 或 `ACn：人工确认 <可观察行为>`。
> 写不出命令的验收标准说明任务还没想清楚 —— 改用 recon 模式先调研。

- [ ] AC1：
- [ ] AC2：
- [ ] AC3：

## 契约影响

- 是否改动跨模块接口 / 数据模型 / 错误码：否 / 是（是则列出 `docs/contracts/` 中要同步的文件）
- 使用方是否已全部同步：是 / 否 / 不涉及

## 范围（哨兵块由 scripts/scope-check.mjs 解析，不要改标记文字）

<!-- SCOPE:MODIFY:BEGIN -->
TASK.md
<!-- SCOPE:MODIFY:END -->

<!-- SCOPE:CREATE:BEGIN -->
<!-- SCOPE:CREATE:END -->

<!-- SCOPE:DENY:BEGIN -->
AGENTS.md
CONSTITUTION.md
ARCHITECTURE.md
ROADMAP.md
{{MANIFEST}}
scripts/
.agents/
.github/
docs/contracts/
# 依赖锁文件按栈选：package-lock.json / poetry.lock / go.sum / Cargo.lock
# 数据库迁移目录
# 凭据与环境文件：.env* / *.pem
<!-- SCOPE:DENY:END -->

填写规则：

- 目录以 `/` 结尾，单文件写完整相对路径，一行一个；不要项目符号、不要反引号。
- 允许新建的文件必须写进 `SCOPE:CREATE`，否则算越界。
- `TASK.md` 必须在 MODIFY 里（否则更新进度就违规）；它本身被改动时门禁会打印提醒，请人工核对白名单没有被扩大。
- **白名单为空时任何改动都算越界（fail-closed）**，这是故意的。

## 计划（拆到"能一眼看出做完了"）

> 每步给：精确文件路径、验证命令、预期结果。计划要细到"给一个不了解项目的人也能照着做"。
> 计划变了先改这里并写明原因，不要在对话里默默改。

- [ ] 步骤 1：改 `<文件路径>` —— 验证：`<命令>` —— 预期：`<结果>`
- [ ] 步骤 2：
- [ ] 步骤 3：

## 轮次记录

> 每轮开始写复述块。旧轮次保留，不要覆盖。若第 3 轮的目标已与最初不同，说明任务在漂移，停下来确认。

### 第 1 轮 {{DATE}}

- 目标：
- 非目标：
- 验收标准：
- 允许修改：
- 禁止修改：
- 风险：
- 计划修改文件及理由：

## 进度日志

> 改完一个文件 / 走完一步就追加一行，不要事后补写。

- {{DATE}} 10:00：完成步骤 1。修改：`<文件>`。验证：`<命令>`。下一步：步骤 2。
- {{DATE}} 10:30：步骤 2 受阻，标记 blocked。原因：……。需要：……

## 红绿证据（涉及行为变化时必填）

> 修 bug、加功能、改行为都要有：先失败的证据，再通过的证据。没有复现证据就不要改代码。

- 红：`<命令>` → `<失败输出摘要>`（时间：）
- 绿：`<命令>` → `<通过输出摘要>`（时间：）
- 不适用（纯文档 / 重构且测试已覆盖）：说明原因

> **行为不变类改动**（重构 / 改名 / 格式化）没有"红"可看，但要给**等价性证据**（配方见
> `references/verification.md` 二·补）：全量测试**通过数一字不差**、`git diff` 里新增的
> `allow` / `skip` / `unwrap` 计数为 0、改前数字用临时工作树在同一条命令下复测过。

## 数字口径

> 卡面、计划、验收表里的每个数字都要跟一条可复制的命令与口径，别只写"约 N 处"。

- `<数字>` ← 命令：`<命令>`（口径：生产目标 / 含测试目标 / 全目标 / …）

## 变更文件

| 文件 | 变更类型 | 说明 |
|---|---|---|
| `<路径>` | 修改 / 新增 / 删除 | 一句话 |

## 测试变更申报

> 只要动过测试文件就填这里；不填视为越界。

- 无。

## 两阶段评审（见 .agents/checklists/task-review.md）

- 第一阶段：规格符合性（AC 是否逐条满足、有无超范围）——结论：
- 第二阶段：代码质量（可读性、命名、抽象、错误处理、一致性）——结论：
- 阻断项是否已清零：是 / 否（否 → 状态保持 in_progress）

## 验证

- 命令：`scripts/verify.ps1 --full`（或 `scripts/verify.sh --full`）
- 日志：`var/verify-<时间戳>.log`
- 结果：通过 / 失败
- scope-check：通过 / 失败
- consistency-audit：0 error / n error（warning 逐条说明）

## 范围变更请求

> 需要动白名单外的文件时写在这里，然后**停下来等确认**，不要自行扩权。

- （无）

## 流程偏差申报

> 卡面是过程记录，不是理想流程的复述。漏了复述就动代码、越界后才补手续、临时探针插入又删除、
> 参数记错导致门禁假红 —— 都写在这里：**事实 / 影响 / 缓解 / 后续做法**。
> 不要用"事后补记"淡化；下一轮的人（和新会话的自己）要靠它判断本卡结论的可信度。

- （无）

## 规则变更请求 / 宪法修正案

> 发现 `AGENTS.md` 或 `CONSTITUTION.md` 有问题时写在这里。Agent 不得直接改这两个文件。

- （无）

## 风险与未决问题

- （无）

## 归档

- 归档到：`{{TASKS_REL}}/<任务 ID>-<短横线标题>.md`（完成后由 Agent 执行）
- 同时更新：`ROADMAP.md` 的任务与模块状态、相关模块 `README.md` 的核验日期
