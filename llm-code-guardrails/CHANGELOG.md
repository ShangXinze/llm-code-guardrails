# 变更记录（用出来的坑）

本文件记录**来自真实使用**的修改：某个仓库踩到的症状 → 改法 → 来自哪里。
一次使用只留一行～一块，不要写设计推演（那是 `references/design-rationale.md` 的地盘）。

收尾时问一句："本次有没有撞到规则 / 模板 / 门禁脚本自身的毛病？" 有就往下追加。
**改完技能本体，记得同步项目里的那份副本**（新项目会自动用新版本，老项目不会）。

追加格式：

```
## <日期> <一句话标题>
- 症状：谁在什么场景下看到什么（要能复现）
- 改法：改了哪个文件、改成什么
- 来源：<仓库名> <任务卡 ID>
```

---

## 2026-09-17 配置单点 + doctor + init 档位（阶段 1）

- 症状：路径仍靠 `verify.*` 变量 + 手工 CLI 三参数，假错误风险未根除；新仓库冷启动过重；
  英文/自定义文档小节名写死在审计里；自测数字写死进文档会腐化。
- 改法：① 新增 `.guardrails.json` 与 `scripts/guardrails-config.mjs`，
  `scope-check` / `consistency-audit` / `verify.*` / `doctor` 统一读配置，CLI 只覆盖；
  ② 新增 `scripts/doctor.mjs`（只读体检）及自测；③ `init_repo --level=full|scope-only|lint-only|minimal`
  （`--minimal` 仍兼容）；④ 审计 `docSections` 可配置；⑤ scope-check 的 git 集成用例在无 git 时 skip。
- 来源：配置单点与 doctor 落地（阶段 1）

## 2026-09-16 归档目录改名后门禁假红（P0）

- 症状：仓库把归档目录本地化成 `docs/任务归档/` 后，全量门禁报
  `M1 已标 done，但 docs/tasks/ 下找不到包含该模块 ID 的归档任务卡`（9 个模块全中）。
  真实原因是 `verify.sh` / `verify.ps1` 调审计时不传 `--tasks`，审计用的是模板默认 `docs/tasks`。
  危险性在于它**看起来像"真缺卡片"**，最容易把人推向造空卡片或放宽审计规则 —— 两条都是硬禁止。
- 改法：① `verify.sh` / `verify.ps1` 增加 `{{ROADMAP_REL}}/{{TASKS_REL}}/{{EVIDENCE_REL}}` 三个
  变量（单一来源）并传给审计；② `init_repo.mjs` 增加 `--taskdir/--roadmap/--evidence`，
  目录按新名铺、模板里的 `docs/tasks/` 引用全部改为占位符；③ 审计在归档目录不存在或为空、
  且确有 done 模块找不到卡片时，先打印一条诊断（含候选目录，按"文件里有没有模块 ID 词元"排序）
  并提示"路径可能没对上"，判定仍是 fail-closed。
- 来源：某生产仓库 G-3b（手工跑审计时 1 error，加 `--tasks docs/任务归档` 立刻 0 error）

## 2026-09-16 DENY 优先于 CREATE，白名单可写出"死信"（P0）

- 症状：卡面 `SCOPE:DENY` 写了整目录 `docs/`、`SCOPE:CREATE` 写了 `docs/任务归档/`，
  归档卡一落盘就被判 `禁止修改（DENY）`。判定顺序是 DENY 优先，CREATE 项是死信，
  但原来的脚本与文档都没提这件事。
- 改法：① `scope-check.mjs` 在 DENY 的目录项覆盖 CREATE/MODIFY 项时打印"死信"警告（判定不变）；
  只比较非通配项，`crates/*/Cargo.toml` 这类 glob 不参与，避免误报；
  ② `rules-task.md` 增加「DENY 的语义」小节与差→好写法对照；③ 两个门禁自测各加用例
  （scope-check 9→11，consistency-audit 24→26）。
- 来源：某生产仓库 G-3b

## 2026-09-16 clippy 配方自相矛盾 + 复杂度计数口径（P1）

- 症状：`readability.md` 的示例 `Cargo.toml` 里写着 `unwrap_used = "warn"`，
  同一页下方又写明"`--deny warnings` 会把新加的 warn 级规则也变成失败" —— 照抄示例的仓库当天就红。
  另：把 `cognitive_complexity` 标成"阈值太松"不准，实测 clippy 1.98 **不累加嵌套层级**：
  "4 层 `for` 嵌套 + `if` + `match` + 臂内 `if/else` + `while` + 布尔序列"只算 9 分，
  凑到 25 分要有 25 个决策点。它兜不住深层嵌套，管嵌套的是 `excessive_nesting` / `max-depth`。
- 改法：示例改为"先量到 0 再写进来"，`warn` 那行注释掉并说明原因；补一段计数口径说明；
  "先测量"步骤补上口径要求（生产目标 vs `--all-targets` 实测差 63 / 312）。
- 来源：某生产仓库 G-3a / G-3b

## 2026-09-16 缺四类配方：重构证据、取数口径、共享工作区、子代理（P1）

- 症状：① 纯重构（改名/抽取）没有"红"可看，`verification.md` 只写了"不适用"；
  ② 卡面与 `ROADMAP.md` 记数不带命令口径，害得任务粒度判断错、拆卡返工；
  ③ 用 `pkill -f` 清自己的残留，顺手杀掉了别人跑了 35 分钟的验证进程（技能里 `pkill` 零命中）；
  ④ 一张卡内派 5 个并行子代理分片重构效率很高，但技能对 `subagent` 零命中，
  而"一次只做一张卡"很容易被读成"不许并行"。
- 改法：`verification.md` 增「行为不变类改动的等价性证据」（通过数不变 / 新增 allow·unwrap 为 0 /
  临时工作树复测改前数字）与「数字都要跟命令和口径」；`AGENTS.md` 模板增 §7.5 共享工作区纪律
  （只按记录的 PID 收尾、禁 `pkill`、临时端口段、临时工作树自己删），§5 增子代理并行与流程偏差申报；
  `rules-task.md` 增「错误 4：数字没有命令与口径」「一张卡内可以并行子代理，但有四条约束」
  「流程偏差要如实写进卡面」；`TASK.md` 模板增「数字口径」与「流程偏差申报」两节。
- 来源：某生产仓库 G-3b（并行子代理）与 M5-1（误杀进程）

## 2026-09-16 脚手架不铺 .gitignore，第一次 verify 就红（P0）

- 症状：新铺的仓库第一次跑 `verify.sh --full` 时，脚本自己 `mkdir -p var` 落盘证据日志，
  范围锁把这个新出现的 `var/` 判成"越界（不在 MODIFY / CREATE 白名单）"。
  用户看到的是"我什么都没改，门禁就说我越界"，而真实原因是证据目录没被 git 忽略。
- 改法：模板增加 `.gitignore`（忽略 `var/` 并写明原因，附一句"各栈构建产物自行追加"），
  进 `--minimal` 保留集；`init_repo` 的"下一步"清单加一条"核对 .gitignore 忽略了证据日志目录"。
  已存在的 `.gitignore` 不会被覆盖（默认跳过）。
- 来源：某生产仓库（它的 `.gitignore` 早在初始提交里就写了 `var/`，但这条经验没进模板）

## 2026-09-16 流程偏差无处记录（P2）

- 症状：本卡面在把代码改完之后才补写（复述块晚于改动），违反"未完成复述禁止改代码"；
  这类偏差在旧模板里没有固定位置，只能塞进「风险与未决问题」，容易被读成"事后补记"。
- 改法：`TASK.md` 模板增「流程偏差申报」小节（事实 / 影响 / 缓解 / 后续做法）；
  `finish.md` 收尾动作增"顺手申报流程偏差"一步。
- 来源：某生产仓库 G-3b

## 2026-09-16 缺少回流机制（P2，本文件本身）

- 症状：本技能被一个 9 crate 的 Rust 仓库连续用了 13 张任务卡，用出来的修补全部留在仓库侧：
  `scope-check.mjs` / `consistency-audit.mjs` 与两个自测**逐字节仍与模板相同**，
  只有 `verify.sh` 长成了项目自己的 `check.sh`（57 → 79 行，多出 release build、Python 门禁、
  测试连跑 2 次，以及那条 `--tasks` 本地补丁）。技能自身也没有变更记录，"下次记得改"靠人脑。
- 改法：新增本文件；`finish.md` 增"收尾必问一句：有没有撞到规则/模板/脚本自身的毛病"；
  `verification.md` 收尾清单增一行"与规则/脚本有关的问题已记入本轮回流"。
- 来源：某生产仓库（13 张任务卡的收口）

## 2026-09-16 工作树放 /tmp 会撑爆 tmpfs，把门禁红成"代码问题"（P1）

- 症状：并发隔离时把 git 工作树建在 `/tmp/g9-sync`，而该机 `/tmp` 是 **2GB 的 tmpfs**；
  Rust 工作区构建产物把 tmpfs 写到 100%，`cargo test` 以
  `failed to write query cache ...: No space left on device` 失败 —— 门禁报红，
  看着像代码问题，实际是磁盘问题（排查顺序也会因此走偏）。
  更糟的是 tmpfs 属于全局共享资源：它满着的时候**别人的进程也会写失败**。
- 改法：写进 `AGENTS.md` 模板 §7.5（共享工作区纪律）的临时目录那一条 ——
  **工作树/构建目录不要放 `/tmp`（tmpfs 通常很小），放仓库同盘的目录**；
  工作树搬家时先把变更文件暂存到同盘目录、`git worktree remove --force` 后重建、逐文件 `diff` 校验。
- 来源：某生产仓库 G-9（护栏回流卡）

## 待办（本轮没做，别假装完整）

- 测试诚信检查（新增 skip/only/无 ID allow 且未申报）仍未落地——阶段 2。
- pre-commit MVP（复述时间戳 + SCOPE 基线哈希）未实现。
- 多活跃任务卡 / 文件 claim 未做（并发仍靠分支隔离）。
- `--taskdir` 只改名了"归档任务卡目录"。若仓库把 `docs/decisions/` 换成单文件
  （如 `docs/决策记录.md`）、`docs/contracts/` 换成聚合文档，仍要手工调整模板引用与注册表 `doc` 列；
  这类"文档结构本地化"目前靠 `--minimal` + 人工，未做参数化。
- 复述门禁仍是散文（"未完成复述禁止改代码"）—— 唯一能机器化的办法是宿主侧 hook
  （编辑非文档文件时若 `TASK.md` 没有本轮复述块就拒绝），未实现。
